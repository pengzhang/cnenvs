# Application dependencies

require:
    - play
    - core -> core latest.integration
    - cms -> cms latest.integration
    - pay -> pay latest.integration
    - mall -> mall latest.integration
    
repositories:
    - HM55Modules:
        type: local  
        artifact: "${application.path}/../module/[module]"  
        contains:
            - core
            - cms
            - pay
            - mall